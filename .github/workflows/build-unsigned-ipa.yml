name: Build Unsigned iOS IPA (Noor)

on:
  push:
    branches: [ main, master, NoorAliIOS, aliandroidv2 ]
  workflow_dispatch:

jobs:
  build-ipa:
    name: Build Unsigned IPA File
    runs-on: macos-14

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Clean install dependencies
        run: |
          rm -f package-lock.json
          npm install --legacy-peer-deps

      - name: Re-run postinstall to ensure SPM version sync
        run: node scripts/inject-speech-recognition-spm.mjs

      - name: Build Web Dist Assets
        run: npm run build

      - name: Capacitor sync iOS
        run: npx cap sync ios

      - name: Verify SPM version alignment
        run: |
          echo "=== CapApp-SPM Package.swift ==="
          cat ios/App/CapApp-SPM/Package.swift
          echo ""
          echo "=== Speech Recognition Package.swift ==="
          cat node_modules/@capacitor-community/speech-recognition/Package.swift

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Build Xcode Archive (Unsigned)
        working-directory: ios/App
        run: |
          xcodebuild -project App.xcodeproj \
            -scheme App \
            -sdk iphoneos \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            DEVELOPMENT_TEAM="" \
            archive -archivePath ../../build/App.xcarchive \
            2>&1 | tail -100

      - name: Package Unsigned IPA
        run: |
          mkdir -p Payload
          cp -r build/App.xcarchive/Products/Applications/*.app Payload/
          zip -r Noor_Khutbah_unsigned.ipa Payload

      - name: Upload Unsigned IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Noor-Khutbah-Unsigned-IPA
          path: Noor_Khutbah_unsigned.ipa
          retention-days: 30
